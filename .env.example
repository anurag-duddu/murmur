# Keyhold Environment Variables
# Copy this file to .env and fill in your actual values

# =============================================================================
# REQUIRED: WorkOS Authentication
# =============================================================================
# Get these from: https://dashboard.workos.com
#
# NOTE: Currently using WorkOS staging/test environment.
# For production, update to production WorkOS credentials.

WORKOS_CLIENT_ID=client_xxx
WORKOS_API_KEY=sk_xxx

# =============================================================================
# REQUIRED FOR DEBUG BUILDS: Groq API
# =============================================================================
# Get your key at: https://console.groq.com
#
# Used for:
# - Speech-to-text (Whisper API)
# - Text enhancement/transformation (LLaMA 3.3 70B)
#
# NOTE: Release builds use a Cloudflare proxy that provides the API key,
# so this is only needed for development (npm run tauri:dev)

GROQ_API_KEY=gsk_xxx

# =============================================================================
# OPTIONAL: App Configuration
# =============================================================================

# Recording mode: "push-to-talk" or "toggle"
# DEFAULT_RECORDING_MODE=push-to-talk

# Default hotkey (examples: "Option+Space", "Cmd+Shift+D")
# DEFAULT_HOTKEY=Option+Space

# Max recording duration in seconds (default: 1800 = 30 minutes)
# MAX_RECORDING_DURATION=1800

# =============================================================================
# OPTIONAL: GitHub (for git push authentication)
# =============================================================================

# GITHUB_TOKEN=ghp_xxx
